import {useState} from 'react';
import {classifyBillItem,parseBillCsv} from '../lib/bill-rules';
export default function BillValidator(){const [rows,setRows]=useState([]);const [file,setFile]=useState('');const load=e=>{const f=e.target.files?.[0];if(!f)return;setFile(f.name);const reader=new FileReader();reader.onload=()=>setRows(parseBillCsv(reader.result));reader.readAsText(f);};const results=rows.map(r=>({...r,...classifyBillItem(r.itemName,r.currentAccount)}));return <main><header><div><p className="eyebrow">ACCOUNTING CONTROL</p><h1>Bill Validator</h1><p className="lead">Check purchase account heads. Rice stays under Groceries purchases; only ice, charcoal and wood go to Other Purchases.</p></div></header><section className="uploads"><label className="upload"><span>+</span><strong>Upload purchase bill</strong><small>{file||'CSV file'}</small><input type="file" accept=".csv" onChange={load}/></label></section>{results.length>0&&<section className="panel"><div className="panelhead"><h2>Validation results</h2><b className="badge">{results.length} items</b></div><div className="table"><table><thead><tr><th>Date</th><th>Vendor</th><th>Item</th><th>Current account</th><th>Suggested</th><th>Verdict</th><th>Reason</th></tr></thead><tbody>{results.map((r,i)=><tr key={i}><td>{r.date}</td><td>{r.vendor}</td><td>{r.itemName}</td><td>{r.currentAccount}</td><td>{r.suggestedAccount}</td><td className={r.verdict==='CORRECT'?'good':r.verdict==='MISMATCH'?'bad':''}>{r.verdict}</td><td>{r.reason}</td></tr>)}</tbody></table></div></section>}</main>}
